services:
  - type: web
    name: nivatier-chatbot
    env: python
    plan: free   # or starter / standard, depending on your preference
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 8000
    autoDeploy: true